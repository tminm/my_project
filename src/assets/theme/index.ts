const theme = {
  color: {
    primaryColor: "red",
    secondaryColor: "#00848A",
  },
  textColor: {
    primaryColor: "#484848",
    secondaryColor: "#222",
  },
  minxs: {
    boxShodow: `transition: box-shadow 250ms ease;
    &:hover {
      box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.18);
    }`,
  },
};

export default theme;
